# AI Assistant Gateway（预留）

这是第二阶段的预留目录。第一阶段不需要启动服务器，Flutter App 使用本地 `MockAssistantProvider` 即可运行。

未来网关负责：

- 接收手机 App 的问题和用药统计摘要；
- 隐藏 API Key；
- 对接小智兼容后端或其他大模型服务；
- 统一返回文字回答；
- 记录必要的会话状态和错误日志。

网关不得允许 AI 直接修改用药剂量、删除原始记录或关闭安全提醒。
