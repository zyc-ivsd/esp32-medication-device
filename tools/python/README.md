# Python 辅助工具

Python 工具运行在电脑上，不是手机 App 的运行依赖。

计划包含：

- BLE 扫描、连接和 Notify 调试；
- CRC、数据包和 ACK 流程测试；
- 模拟数据生成；
- CSV/JSON 数据分析和绘图。

安装依赖：

```bash
python -m venv .venv
pip install -r requirements.txt
```
